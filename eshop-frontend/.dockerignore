# Exclude local node_modules — Docker installs fresh Linux-compatible ones via npm ci
node_modules/

# Exclude build output — Docker builds fresh
dist/
build/

# Exclude env files
.env
.env.*

# Exclude git
.git/
.gitignore
