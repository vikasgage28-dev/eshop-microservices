apiVersion: v1
kind: Service
metadata:
  name: ordering-api
  namespace: eshop
spec:
  type: ClusterIP
  selector:
    app: ordering-api
  ports:
    - name: http
      port: 80
      targetPort: 8080