apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sql-server-pvc
  namespace: eshop
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: sql-server-hdd
  resources:
    requests:
      storage: 32Gi
